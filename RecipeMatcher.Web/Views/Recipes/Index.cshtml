@model IReadOnlyList<RecipeMatcher.Web.Models.Recipe>

<h1>Recipes</h1>

<ul>
    @foreach (var recipe in Model)
    {
        <li>@recipe.Name, @recipe.PreparationMinutes minutes <a asp-action="Details" asp-route-id="@recipe.Id">Details</a></li>
    }
</ul>